name: wave_env
description: "The Desktop Environment for Wave OS"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.0


dependencies:
  flutter:
    sdk: flutter
  window_manager: ^0.5.0
  google_fonts: ^6.1.0
  material_symbols_icons: ^4.2.7
  quick_usb: ^0.3.1
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true